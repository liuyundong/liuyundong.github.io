# AGENTS.md

## 项目约定

- 项目以 `index.html` 作为唯一页面入口，保证页面可以直接部署到静态托管服务。
- CSS 和 JavaScript 必须直接写入 `index.html`，不要拆分为额外的 `.css` 或 `.js` 文件，除非用户明确要求改变这一约定。
- 不引入构建工具、框架或运行时依赖；页面应在无需安装依赖的情况下运行。
- `index.html` 禁止引用外部 HTTP 资源，不使用 `http://`、`https://`、外部字体、远程脚本、远程样式或 CSS `url(...)` 资源。
- 保留现有页面的视觉语言和交互方式，避免无关的重构、格式化或依赖升级。

## 移动端布局

- 页面必须优先适配手机屏幕，并使用全屏布局覆盖可用视口。
- 使用 `100svh` 或等效的动态视口单位处理移动端浏览器地址栏变化，避免内容被截断。
- 适配刘海屏和底部手势区域，使用 `env(safe-area-inset-top)`、`env(safe-area-inset-right)`、`env(safe-area-inset-bottom)` 和 `env(safe-area-inset-left)`。
- 页面不能出现横向滚动条；内容、按钮和交互区域必须在常见手机宽度下完整可见。
- 为触控提供稳定的布局尺寸和足够大的点击区域，避免依赖 hover 才能完成核心操作。
- 保证桌面端仍可使用键盘或鼠标完成核心交互，并保持合理的响应式布局。

## 页面缩放

- 在 `index.html` 中保留移动端 viewport 配置：
  `width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no, viewport-fit=cover`。
- 页面不得通过双指缩放或浏览器手势改变布局比例。
- 使用 `touch-action` 和必要的触摸事件处理避免误触发页面滚动或缩放，同时不能阻断正常的按钮点击。

## HTML、CSS 与 JavaScript

- 使用语义化 HTML、清晰的 `id` 和 `class` 命名，避免用无意义的一字母变量名。
- 所有可交互控件都应有明确的可见状态和 `aria-label` 或可读文本。
- 颜色、字号、间距和层级关系应通过 CSS 变量集中管理，避免重复的魔法值。
- 处理窄屏、矮屏、横屏和较大字体等情况，保证文字不溢出、不遮挡相邻内容。
- JavaScript 初始化时应处理页面加载、重置、暂停、错误状态和重复触发等边界情况。
- 不使用内联事件属性；事件监听统一放在内嵌脚本中。
- 不在代码中加入与实现无关的注释、日志或调试内容。

## 验证要求

- 每次修改后检查 `index.html` 是否仍然存在，且 viewport 配置没有被移除。
- 检查 `index.html` 不包含 `http://`、`https://` 或外部资源引用。
- 对内嵌 JavaScript 执行语法检查，并确认编辑器没有新增错误。
- 在手机尺寸和桌面尺寸下检查首屏布局、交互按钮、滚动行为和页面缩放行为。
- 不要为了修复与当前任务无关的问题修改其他文件。
